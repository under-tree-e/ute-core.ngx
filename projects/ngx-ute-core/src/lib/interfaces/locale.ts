/**
 * Locale object
 * @prop {@link LocaleData.code | code}: `string` - String identifier
 * @prop {@link LocaleData.text | text}: `string` - Translate string
 */
export interface LocaleData {
    code: string;
    text: string;
}
