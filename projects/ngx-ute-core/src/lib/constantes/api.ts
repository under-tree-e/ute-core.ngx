/**
 * ApiConst constant
 *
 * An object mapping string keys to their corresponding string values used in API requests.
 *
 * @constant
 *
 * @prop {string} mt - Method key in API requests.
 * @prop {string} tb - Table key in API requests.
 * @prop {string} st - Select key in API requests.
 * @prop {string} wr - Where key in API requests.
 * @prop {string} or - Order key in API requests.
 * @prop {string} no - No Reference key in API requests.
 * @prop {string} lt - Limit key in API requests.
 * @prop {string} rf - References key in API requests.
 */
export const ApiConst = {
    mt: "method",
    tb: "table",
    st: "select",
    wr: "where",
    or: "order",
    no: "noref",
    lt: "limit",
    rf: "refs",
};
